long long missingNum(int arr[], int size) {
    long long n = size + 1;   // total numbers from 1 to n
    long long sum = 0;

    for (int i = 0; i < size; i++) {
        sum += arr[i];
    }

    long long expectedSum = (n * (n + 1)) / 2;

    return expectedSum - sum;
}
